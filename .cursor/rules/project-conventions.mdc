---
description: Ncp.Admin 项目总则与规范文档索引，做功能开发时优先遵循
alwaysApply: true
---

# Ncp.Admin 项目规范

本仓库采用 **DDD 分层架构**（NetCorePal）。进行功能开发时，**请先阅读并遵循**以下规则文件（`.cursor/rules/*.mdc`），根据开发内容选读对应文件。

## 项目结构

```
Ncp.Admin.sln
├── src/
│   ├── Ncp.Admin.Domain/         # 领域层 - 聚合根、实体、领域事件
│   ├── Ncp.Admin.Infrastructure/ # 基础设施层 - EF配置、仓储接口、仓储实现
│   └── Ncp.Admin.Web/           # 表现层 - API、应用服务
└── test/                            # 测试项目
    ├── Ncp.Admin.Domain.Tests/            # 领域层测试项目
    ├── Ncp.Admin.Infrastructure.Tests/    # 基础设施层测试项目
    └── Ncp.Admin.Web.Tests/              # 表现层测试项目
```

**分层依赖关系：** Web → Infrastructure → Domain（严格单向依赖）

## 强制要求（必须遵守）

- 聚合根使用强类型 ID，**不手动赋值 ID**（EF 值生成）
- 每个命令都有验证器，验证器继承 `AbstractValidator<T>`
- 业务异常使用 `KnownException`，不用普通 `Exception`
- 命令处理器**不调用 SaveChanges**
- 仓储全部使用异步方法（GetAsync、AddAsync 等）
- 依赖方向：Web → Infrastructure → Domain（严格单向）
- 优先使用主构造函数
- 优先使用 `await` 关键字和 Async 方法
- 领域事件处理器实现 `Handle()` 方法而不是 `HandleAsync()`
- FastEndpoints 使用构造函数注入 `IMediator`，使用 `Send.OkAsync()` 和 `.AsResponseData()`
- 强类型 ID 直接使用类型，避免 `.Value` 属性，依赖隐式转换

## 后端开发时

请根据任务打开并遵循对应的规则文件，详见 `.cursor/rules/backend-ddd.mdc`。**新增需权限的接口时**，后端需同步 PermissionCodes、PermissionDefinitionContext、PermissionMapper、端点权限、可选 Seed。

## 前端开发时

前端为 Vben Admin（Vue 3 + Ant Design Vue）。**新增带权限的功能时**，前端需同步 permission-codes.ts、permission-tree.ts、路由 authority，新菜单需在父路由 children 中加子路由（详见 `.cursor/rules/frontend-vben.mdc`）。更完整规范见 `.cursor/skills/ncp-admin-frontend/SKILL.md`。

## GlobalUsings.cs 配置

各层的常用引用已在 GlobalUsings.cs 中全局定义：

- **Web 层**：`FluentValidation`、`MediatR`、`NetCorePal.Extensions.Primitives`（KnownException）、`FastEndpoints`、`NetCorePal.Extensions.Dto`（ResponseData）、`NetCorePal.Extensions.Domain`（领域事件处理器）
- **Infrastructure 层**：`Microsoft.EntityFrameworkCore`、`Microsoft.EntityFrameworkCore.Metadata.Builders`、`NetCorePal.Extensions.Primitives`
- **Domain 层**：`NetCorePal.Extensions.Domain`、`NetCorePal.Extensions.Primitives`（强类型 ID）
- **Tests 层**：`Xunit`、`NetCorePal.Extensions.Primitives`

### 常见手动 using 引用

当 GlobalUsings 未覆盖时，需手动添加：

- **查询处理器**：`using Ncp.Admin.Domain.AggregatesModel.{AggregateFolder};` + `using Ncp.Admin.Infrastructure;`
- **实体配置**：`using Ncp.Admin.Domain.AggregatesModel.{AggregateFolder};`
- **端点**：`using Ncp.Admin.Domain.AggregatesModel.{AggregateFolder};` + `using Ncp.Admin.Web.Application.Commands.{FeatureFolder};` + `using Ncp.Admin.Web.Application.Queries.{FeatureFolder};`

## 异常处理原则

在需要抛出业务异常的地方，必须使用 `KnownException` 而不是普通 `Exception`。`KnownException` 会被框架自动转换为合适的 HTTP 状态码，异常消息会直接返回给客户端。
