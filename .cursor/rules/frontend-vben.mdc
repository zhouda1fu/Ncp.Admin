---
description: 前端 Vben Admin：路径与结构索引；详细与易错点见 ncp-admin-frontend skill
globs: src/frontend/**/*.vue, src/frontend/**/*.ts
alwaysApply: false
---

# 前端 Vben Admin

通用仓库规范以 `AGENTS.md` 为准。

本规则只做前端入口索引。涉及页面、API、路由、权限树、表单抽屉、列表 schema 或菜单高亮时，优先参考 **`.cursor/skills/ncp-admin-frontend/SKILL.md`**。

## 路径与结构（相对 `src/frontend/apps/admin-antd/`）

- API：`src/api/system/{feature}.ts`
- 列表 / 表单 / Schema：`src/views/{module}/{feature}/` 下 `list.vue`、`modules/form.vue`、`data.ts`
- 路由：`src/router/routes/modules/{module}.ts`
- 多语言：`src/locales/langs/{zh-CN,en-US}/{module}.json`

## 何时切到详细 skill

- 新增或修改受权限控制的页面、按钮、菜单
- 调整 `data.ts` 中的搜索表单、列表列、操作列
- 处理 `hideInMenu` 子路由与 `activePath`
- 编写或修改抽屉表单，尤其是 `useVbenDrawer + useVbenForm`
- 新增 `views + api + routes + locales` 成套功能
