---
description: 后端 DDD 开发顺序与文件位置，实现前请读对应规则文件
globs: src/**/*.cs
alwaysApply: false
---

# 后端 DDD 开发

进行聚合、命令、查询、端点等开发时，**必须先阅读**对应的规则文件（`.cursor/rules/*.mdc`）：

| 开发内容 | 对应规则文件 |
|----------|-------------|
| 聚合根与强类型 ID | `aggregate.mdc` |
| 领域事件 | `domain-event.mdc` |
| 仓储 | `repository.mdc` |
| 实体配置 | `entity-configuration.mdc` |
| DbContext | `dbcontext.mdc` |
| 命令与命令处理器 | `command.mdc` |
| 查询与查询处理器 | `query.mdc` |
| 端点 | `endpoint.mdc` |
| 领域事件处理器 | `domain-event-handler.mdc` |
| 集成事件 | `integration-event.mdc` |
| 集成事件转换器 | `integration-event-converter.mdc` |
| 集成事件处理器 | `integration-event-handler.mdc` |
| 单元测试 | `unit-testing.mdc` |

## 开发顺序（按此顺序执行）

1. 定义聚合、实体与强类型 ID  
2. 定义领域事件  
3. 仓储接口与实现  
4. 实体配置与 DbContext  
5. 命令与命令处理器（含验证器）  
6. 查询与查询处理器  
7. Endpoints  
8. 领域事件处理器  
9. 如需跨限界上下文：集成事件、转换器、处理器  

## 文件位置

| 类型 | 路径 |
|------|------|
| 聚合根 | `src/Ncp.Admin.Domain/AggregatesModel/{AggregateName}Aggregate/` |
| 领域事件 | `src/Ncp.Admin.Domain/DomainEvents/` |
| 仓储 | `src/Ncp.Admin.Infrastructure/Repositories/` |
| 实体配置 | `src/Ncp.Admin.Infrastructure/EntityConfigurations/` |
| 命令/处理器 | `src/Ncp.Admin.Web/Application/Commands/{Module}/` |
| 查询/处理器 | `src/Ncp.Admin.Web/Application/Queries/` |
| 端点 | `src/Ncp.Admin.Web/Endpoints/{Module}/` |
| 领域事件处理器 | `src/Ncp.Admin.Web/Application/DomainEventHandlers/` |

## 新增带权限的接口时（后端必做）

1. `AppPermissions/PermissionCodes.cs` 新增权限常量（与前端 permission-codes.ts 一致）
2. `PermissionDefinitionContext.cs` 对应父权限下 `AddChild(权限码, "显示名")`
3. `PermissionMapper.cs` 的 `_permissionDescriptionMap` 增加条目
4. 端点绑定：`Permissions(PermissionCodes.XxxView)` 等
5. 需默认给管理员时：`SeedDatabaseExtension.cs` 的 `adminPermissionCodes` 加入新权限码

## 端点 Request/Response 要点

- 使用 **record**，ID 用强类型（CustomerId、UserId 等），禁止裸 Guid/long/string
- Request/Response 与端点类需有 `<summary>` 和参数的 `<param>` 文档注释
- 响应用 `ResponseData<T>`，发送用 `.AsResponseData()`。详细格式见 `.cursor/skills/ncp-admin-ddd/SKILL.md`
